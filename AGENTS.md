# Contribution and commit identity

This is Balaji Royal's portfolio repository.

## Commit authorship

- Every commit must use Balaji's Git identity:
  - Name: `Balaji Royal`
  - Email: `Balajiroyalterla007@gmail.com`
- AI tools, including Codex, Claude, and any other agent, must not use an AI, bot, assistant, or tool identity as the commit author.
- Before committing, verify `git config user.name` and `git config user.email`. If they are wrong, stop and ask Balaji before changing them.
- AI assistance may be documented in the task or pull request when useful, but it must not replace Balaji's commit authorship.
